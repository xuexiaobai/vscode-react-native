import * as appStrategy from "./appStrategy";
import * as appDebugMode from "./appDebugMode";

export module iOS {
    export var AppStrategy = appStrategy.AppStrategy;
    export var AppDebugMode = appDebugMode.AppDebugMode;
}
